# This file activates the 'local' Spring profile, which configures the application
# to use LocalStack instead of real AWS services.
# To use it, simply ensure this file exists in your `src/main/resources` directory.

# Activate the 'local' profile
spring.profiles.active=local

# Standard LocalStack endpoint URL
aws.endpoint=http://localhost:4566
